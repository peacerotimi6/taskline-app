# AWS

## Elastic Beanstalk (Node platform)
1. Use `deploy/aws/Procfile`.
2. Deploy as a Node.js application.
3. Ensure the environment provides `PORT` to the app.

Note:
- This repo currently includes only the `Procfile` for AWS.
- There is no `.ebextensions/node.config` in this project.
