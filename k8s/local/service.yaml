apiVersion: v1
kind: Service
metadata:
  name: tasklineapp-service
spec:
  type: NodePort
  selector:
    app: tasklineapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080